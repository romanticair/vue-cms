// 项目入口文件
import Vue from 'vue'
import VueRouter from 'vue-router'
// 导入 App 根组件
import app from './App.vue'
// 导入 TabBar 组件


// 安装路由
new vueRouter = VueRouter({

})



var vm = new Vue({
	el: '#app',
	render: c => c(app)
})
